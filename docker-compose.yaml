version: '3'
services:
  mongodb:
    image: mongo:3.6
    container_name: "mongodb"
    volumes:
      - ./data/db:/data/db
    ports:
      - 27017:27017
    command: mongod --smallfiles --logpath=/dev/null # --quiet
      #  api:
      #    build: '.'
      #    ports:
#      - '8080:8080'
  #    depends_on:
  #      - 'mongo'